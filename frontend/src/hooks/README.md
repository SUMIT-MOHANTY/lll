# Slot Filters for Passport Booking System

This module provides filtering capabilities for the appointment booking system dashboard.

## Components

### SlotFilters

A UI component that renders location dropdown and date picker inputs, allowing users to filter appointment slots.

#### Usage
